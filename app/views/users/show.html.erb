<h1 align="center" class="mt-4 mb-2"> Welcome to <%= @user.username %>'s page</h1>
<div class="row">
	<div class="col-md-2 col-md-offset-2 rounded mx-auto d-bloc ">
		<%= gravatar_for @user, size: 150 %>
	</div>
</div>
<hr>

<h4 class="text-center"> <%= @user.username %>'s articles </h4>

<h6 class="d-flex justify-content-center pl-2 myPagination">
	<%= will_paginate @user_articles ,renderer: BootstrapPagination::Rails %>
</h6>

<%= render "articles/article", obj: @user_articles %>

<h6 class="d-flex justify-content-center pl-2 myPagination">
	<%= will_paginate @user_articles ,renderer: BootstrapPagination::Rails %>
</h6>